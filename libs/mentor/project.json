{
  "name": "mentor",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/mentor/src",
  "projectType": "library",
  "tags": ["scope:mentor", "platform:client", "type:domain"],
  "// targets": "to see all targets run: nx show project mentor --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/mentor/tsconfig.check.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/mentor/**/*.{ts,tsx}"]
      }
    }
  }
}
