{
  "name": "imports",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/imports/src",
  "projectType": "library",
  "tags": ["scope:imports", "platform:client", "type:domain"],
  "// targets": "to see all targets run: nx show project imports --web",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc --noEmit -p libs/imports/tsconfig.check.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "options": {
        "lintFilePatterns": ["libs/imports/**/*.{ts,tsx}"]
      }
    }
  }
}
